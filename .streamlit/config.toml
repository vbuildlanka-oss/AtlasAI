# Churn Radar — brand theme (radar / phosphor-green on deep dark)
[theme]
primaryColor = "#2ee6a6"
backgroundColor = "#0a0e14"
secondaryBackgroundColor = "#121822"
textColor = "#e6e9ef"
font = "sans serif"

[server]
headless = true

[browser]
gatherUsageStats = false
