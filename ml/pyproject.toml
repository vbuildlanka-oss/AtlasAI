[project]
name = "churnlens"
version = "1.0.0"
description = "ChurnLens AI - explainable customer churn model + browser artifacts"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
addopts = "-q"
testpaths = ["tests"]

[tool.ruff]
line-length = 90
